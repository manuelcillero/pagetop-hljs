[package]
name = "hljs_sample"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
pagetop = { version = "0.0", path = "../../PageTop/pagetop" }
pagetop-hljs = { version = "0.0", path = ".." }
